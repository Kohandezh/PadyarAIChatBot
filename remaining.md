> ## ⚠️ این سند کهنه است — CORRECTION NOTICE (2026-08-19)
>
> نوشته‌های زیر مربوط به ۱۴۰۵/۰۵/۲۵ است و چند ادعای آن **دیگر درست نیست**.
> این سه مورد به‌صراحت اصلاح می‌شوند:
>
> 1. **«۲۲۸ تست سبز»** → اکنون **۸۵۸ تست سبز** + ۱۴ تست زندهٔ اختیاری (skip).
> 2. **«پیامک واقعی هرگز ارسال نشده ❌»** → **نادرست.** ارسال از طریق قالب
>    تأییدشدهٔ ۱۶۵۴ کار می‌کند و توسط کاربر تأیید شده است.
> 3. **«PostgreSQL ⬜»** → **انجام شده.** بکاند تولید اکنون PostgreSQL 16 است
>    (`DB_BACKEND=postgres`) با مهاجرت، پشتیبان‌گیری، بازیابی و حالت تعمیر.
>
> وضعیت معتبر و به‌روز در این فایل‌هاست:
> `AI-PROVIDER-CHECKPOINT.md` و `docs/engineering/ai-providers/`.
>
> بخش‌های محتوایی (تاکسونومی، تعارض‌های محتوا، بازبینی انسانی) هنوز معتبرند.

# چه چیزی مانده — سند تحویل

آخرین به‌روزرسانی: ۱۴۰۵/۰۵/۲۵ (2026-08-16)
وضعیت مجموعهٔ تست در این لحظه: **۲۲۸ تست سبز** · دود زندهٔ **۱۱/۱۱**

این سند برای کسی نوشته شده که کار را از اینجا ادامه می‌دهد. صادقانه است: چیزی
که انجام نشده «انجام نشده» نوشته شده، نه «تقریباً آماده».

قبل از هر تغییری این دو را بزنید تا خط پایه داشته باشید:

```bash
.venv/bin/python -m pytest -q                 # ۲۲۸ تست
.venv/bin/python scripts/smoke-live.py        # نیاز به سرور در حال اجرا دارد
```

`smoke-live.py` عمداً ساخته شده: تغییرات hardening از نوعی‌اند که تست واحد را
رد می‌کنند و محصول واقعی را می‌شکنند. بعد از هر تغییر دوباره اجرایش کنید.

---

## ۱. مسدودکننده‌ها — بدون این‌ها نمی‌شود رویداد را اجرا کرد

### ۱.۱ پیامک واقعی هرگز ارسال نشده ❌

**وضعیت:** کل زنجیره ساخته و تست شده، اما **حتی یک پیامک واقعی فرستاده نشده**.

آخرین تلاش با خطای زیر شکست خورد:

```
HTTP 403 — Access Denied (ArvanCloud)
The request has been blocked from your IP or your location!
```

هر دو هاست (`sms.asanak.ir` و `panel.asanak.com`) همین را دادند، در حالی که
`inotex.com` از همان ماشین باز می‌شود. یعنی **WAF آسانک IP این ماشین را بلاک
کرده**، نه اینکه اعتبارنامه غلط باشد.

**کاری که باید بشود:**
1. از سروری که آسانک دسترسی‌اش را می‌پذیرد (یا با IP مجازشده در پنل آسانک) وارد
   `/secure-panel-inotex/settings/sms` شوید.
2. تب **آسانک** را انتخاب کنید (نه «حالت آزمایشی» — وگرنه هیچ پیامکی نمی‌رود).
3. **اول «بررسی اعتبار» را بزنید.** بدون خرج کردن پیامک ثابت می‌کند نام کاربری،
   رمز و هاست درست‌اند.
4. بعد «ارسال پیامک آزمایشی».

**نکتهٔ باز:** شمارهٔ فرستنده‌ای که الان ذخیره شده `9821700009121491644` است —
۱۹ رقم. نمونهٔ خود آسانک ۸ رقم است (`98200049`). به‌نظر می‌رسد شمارهٔ فرستنده و
مقصد به هم چسبیده‌اند. اگر غلط باشد خطای `1002` («شمارهٔ فرستنده فعال نمی‌باشد»)
می‌گیرید. **تأیید نشده — باید بررسی شود.**

**نکتهٔ مهم دربارهٔ کلید API:** در کل مستندات رسمی آسانک **هیچ API key ای وجود
ندارد**. هر چهار endpoint فقط با `username` + `password` در بدنهٔ درخواست کار
می‌کنند. فیلد کلید API نگه داشته و رمزنگاری می‌شود، ولی مسیر ارسال از آن
استفاده نمی‌کند. اگر حساب شما چیز دیگری می‌خواهد، مستنداتش را بیاورید.

**ریسک واقعی که هنوز آزمایش نشده:** خطای `1014` یعنی «شمارهٔ فرستنده مجاز به
ارسال لینک نیست». پیام OTP ما به خط `@host #code` ختم می‌شود (لازمهٔ WebOTP).
اگر فیلتر لینک آسانک این را بگیرد، **کل ثبت‌نام می‌شکند**. تا اولین ارسال واقعی
معلوم نمی‌شود.

### ۱.۲ فایل تاکسونومی مشتری ❌

`data/visit-taxonomy.json` هنوز **placeholder** است (`"status": "placeholder"`).
شغل‌ها، سمت‌ها، علاقه‌مندی‌ها و بخش‌های نمایشگاه را من از محتوای رسمی سایت
استخراج کرده‌ام، نه از فایل مشتری.

زیرساخت آماده است: فایل را جایگزین کنید، **بدون تغییر کد و بدون ری‌استارت** زنده
می‌شود. راهنما: `docs/features/targeted-visit/TAXONOMY.md`.
از پنل هم قابل ویرایش است: `/secure-panel-inotex/settings/taxonomy`.

### ۱.۳ سه تعارض محتوایی باز ❌

در `content/review-queue.md`: **ساعت بازدید**، **فهرست دقیق برگزارکنندگان**،
**مسیر ثبت‌نام بازدید**. تا وقتی باز باشند، ربات در این سه موضوع محتاطانه جواب
می‌دهد.

### ۱.۴ بازبینی انسانی — امضا نشده ❌

**این تنها موردی است که هیچ کدی حلش نمی‌کند.**

کمیتهٔ ارزیابی دانش‌بنیان صریحاً نوشته بود: «مستندات طراحی، تست کافی، **review
انسانی**، تحلیل معماری و **ownership فنی روشن**». شش‌تای اول ساخته شده. اما
«بازبینی انسانی» یعنی **یک مهندس نام‌بُرده باید کد را بخواند و امضا کند**.

چک‌لیستش آماده است: `docs/engineering/HUMAN_REVIEW_CHECKLIST.md`.
تا آن امضا انجام نشود، همهٔ اسناد این پروژه صادقانه می‌نویسند **pending** — و
نباید عوض شوند.

---

## ۲. انجام نشده — تصمیم شماست

### ۲.۱ Content-Security-Policy ⬜

هدرهای امنیتی اضافه شده‌اند (`nosniff`، `Referrer-Policy`، و برای پنل ادمین
`X-Frame-Options: DENY` + `Cache-Control: no-store`). **CSP عمداً اضافه نشده.**

تم‌ها به‌درستی از script و style درون‌خطی استفاده می‌کنند؛ سیاستی که به‌اندازهٔ
کافی سخت باشد که ارزش داشته باشد، چت را می‌شکند. کارِ درستش دادن nonce به آن
بلاک‌هاست. نیمه‌کاره انجام نشد.

### ۲.۲ مانیتورینگ ⬜

لاگ JSON و `/api/ready` هست. **Prometheus / OpenTelemetry نیست.** هیچ ارجاعی به
آن‌ها در `app/` یا `requirements*.txt` وجود ندارد.

### ۲.۳ PostgreSQL / pgvector ⬜

SQLite با WAL برای نصب تک‌مشتری غرفه‌ای مناسب است. مسیر PostgreSQL برای استقرار
سازمانی چندکاربره **طراحی نشده**.

⚠️ **هشدار مهم:** این پروژه **سیستم migration ندارد**. اسکیما با
`CREATE TABLE IF NOT EXISTS` و `ALTER TABLE ADD COLUMN` تکرارپذیر جلو می‌رود.
مسیر ارتقا تست شده و داده از دست نمی‌رود (`tests/test_db_upgrade.py`)، اما هر
تغییر اسکیمای پیچیده‌تر نیاز به یک ابزار migration واقعی دارد.

### ۲.۴ تست e2e مرورگر ⬜

`tests/e2e/` عملاً خالی است. مسیر ثبت‌نام فقط **دستی** در مرورگر تست شده. زیرساخت
`pytest-playwright` نصب است.

### ۲.۵ کتابخانهٔ ویدیو ⬜

`welcome.mp4` و `idle.mp4` هستند و متصل‌اند. **ویدیوی اختصاصی به‌ازای هر رکورد
دانش تولید نشده.**

### ۲.۶ آسیب‌پذیری وابستگی — ریسک پذیرفته‌شده ⚠️

`cryptography 48.0.1` سه advisory دارد. **قابل رفع نیست و قابل بهره‌برداری هم
نیست**؛ هر دو اثبات شده:

- نسخه‌های اصلاح‌شده (۴۹/۵۰) **روی PyPI منتشر نشده‌اند**؛ آخرین موجود ۴۸.۰.۱ است.
- هر سه در PKCS#7 و تأیید زنجیرهٔ X.509 هستند. کل استفادهٔ ما Fernet و HKDF است.
  `grep -rn "pkcs7\|x509" app/` → صفر نتیجه.

جزئیات با دستورهای قابل بازاجرا: `docs/engineering/DEPENDENCY_AUDIT.md`.
**وقتی نسخهٔ ۵۰ منتشر شد ارتقا دهید و آن بخش را حذف کنید.**

### ۲.۷ مستندات محصول دیگر ⬜

`docs/_other-product-padyar-ai/` یک محصول **کاملاً متفاوت** را شرح می‌دهد
(مونوریپوی pnpm/Next.js روی Supabase). منتقل و برچسب‌گذاری شد، **حذف نشد** —
چون ممکن است مستندات واقعی آن محصول باشد. اگر جای دیگری دارند، این پوشه
می‌تواند کامل حذف شود.

---

## ۳. وضعیت گیت — قبل از push بخوانید

مخزن `PadyarAIChatbot/` است (نه پوشهٔ والد). **هیچ چیز کامیت نشده.**

فایل‌های **untracked** که اگر `git add` نشوند از قلم می‌افتند:

```
app/routers/otp.py              ← کل روتر ماژول ثبت‌نام
app/services/{otp,sms,taxonomy,visit_plan,secure_store,bm25,rerank}.py
data/visit-taxonomy.json
templates/admin/settings_{sms,taxonomy}.html
static/admin/js/settings_{sms,taxonomy}.js
static/companion/*
scripts/smoke-live.py
tests/test_*.py (چند فایل جدید)
docs/engineering/DEPENDENCY_AUDIT.md
remaining.md
```

بررسی: `git status --porcelain | grep '^??'`

**هرگز کامیت نکنید:** `.env`، `ADMIN_CREDENTIALS.txt`، `chat_history.db`،
`data/otp-dev-outbox.log`. همه در `.gitignore` هستند — تأیید شده. یک گیت CI هم
اضافه شده که اگر `.env` وارد گیت شود build را قرمز می‌کند.

---

## ۴. چیزهایی که ممکن است غافلگیرتان کند

**۴.۱ تم‌های ادمین کش می‌شوند.** `_jinja_env` با `auto_reload=False` ساخته شده.
تغییر در `templates/admin/*.html` **تا ری‌استارت سرور دیده نمی‌شود**. این عمدی
است (کارایی)، ولی وقت دیباگ گیج‌کننده است.

**۴.۲ اسکریپت‌های `.venv/bin/` قبلاً به پروژهٔ Elecomp اشاره می‌کردند.** ۲۵ اسکریپت
(از جمله `uvicorn`، `pytest`، `pip` و `activate`) شبنگ قدیمی داشتند. اصلاح شد،
ولی اگر venv را دوباره ساختید حواستان باشد.

**۴.۳ `ADMIN_CREDENTIALS.txt` قبلاً با هر اجرای تست بازنویسی می‌شد.** باگ بود و
رفع شد (فایل حالا کنار دیتابیس خودش نوشته می‌شود). اگر رمز ادمین کار نکرد:

```bash
.venv/bin/python scripts/change-admin.py
```

**۴.۴ کلید رمزنگاری از `.env` نمی‌آید.** اعتبارنامهٔ پیامک با کلیدی رمز می‌شود که
از `SECRET_KEY` یا ردیف `app_secret_key` دیتابیس مشتق می‌شود. **اگر `.env` را
تنهایی به سرور دیگری ببرید یا `SECRET_KEY` را بچرخانید، اعتبارنامه‌ها
غیرقابل‌رمزگشایی می‌شوند** و باید دوباره در پنل وارد شوند.

**۴.۵ نوشتن در `.env` از پنل وب.** برنامه به فایل محیطی خودش دسترسی نوشتن دارد.
این خواستهٔ صریح مشتری بود، ولی یک بده‌بستان واقعی است: نشست ادمین لو رفته
می‌تواند پیکربندی روی دیسک را بازنویسی کند، و برنامه نمی‌تواند از کانفیگ
read-only اجرا شود. اگر ترجیح می‌دهید فقط دیتابیس بماند، `save_settings` را
عوض کنید.

---

## ۵. آنچه ساخته و تأیید شده (برای اینکه دوباره نسازید)

| حوزه | وضعیت |
|---|---|
| هویت رویداد قبلی | کامل حذف شده — گیت CI جلوی برگشتش را می‌گیرد |
| بازیابی هیبریدی | BM25 + امبدینگ محلی + بازرتبه‌بند · recall@1 = **0.881**، recall@3 = **0.952**، MRR = **0.919** |
| ثبت‌نام + OTP | HMAC کلیددار، CSPRNG، مقایسهٔ constant-time، تک‌مصرف، سقف تلاش/ارسال |
| برنامه‌ریز بازدید هدفمند | داده‌محور؛ **هرگز نام غرفه‌دار نمی‌سازد** |
| پنل ادمین | صفحهٔ SMS + ویرایشگر تاکسونومی (ساده + JSON خام، با اعتبارسنجی و بکاپ) |
| ذخیرهٔ امن | Fernet + HKDF؛ راز نه در دیتابیس متن ساده است نه در `.env`؛ `.env` با مجوز ۰۶۰۰ |
| ارتقای دیتابیس | تست‌شده روی اسکیمای قدیمی — داده از دست نمی‌رود |
| CI | تست + **گیت ارزیابی (مسدودکننده)** + اسکن راز (مسدودکننده) + pip-audit (گزارشی) |
| هدرهای امنیتی | اعمال‌شده، با تست‌هایی که «کجا نباید باشند» را هم قفل می‌کنند |
| بستهٔ شواهد دانش‌بنیان | `docs/knowledge-based-evidence/` — هر ادعا با فایل و دستور قابل اجرا |

---

## ۶. اگر فقط ۳۰ دقیقه وقت دارید

1. `.venv/bin/python -m pytest -q` → باید ۲۲۸ سبز باشد
2. `python main.py` بعد `.venv/bin/python scripts/smoke-live.py` → باید ۱۱/۱۱ باشد
3. `docs/knowledge-based-evidence/00-executive-summary-fa.md` را بخوانید
4. بند ۱ همین سند را بخوانید — چهار مسدودکننده
5. `git status --porcelain | grep '^??'` و فایل‌های جدید را `git add` کنید
