# گزارش شفاف استفاده از هوش مصنوعی در توسعه

استفاده از کدنویسی با کمک AI پنهان نمی‌شود؛ این سند سابقهٔ آن است.
هر مدخل: چه چیزی، با چه مدلی، و وضعیت بازبینی انسانی.

## نشست ۱۴۰۵/۰۵/۲۳ (2026-08-14) — تبدیل به INOTEX + بلوغ فنی

- **مدل/ارکستراتور:** Claude (claude-fable-5) در نقش منطقی «Fable» — طراحی،
  پیاده‌سازی، ارزیابی و مستندسازی در همین نشست.
- **کارهای انجام‌شده (پچ‌های پذیرفته‌شده):**
  1. حذف کامل هویت رویداد قبلی از working tree (کد، تم‌ها، تست‌ها، DB و env).
  2. seed دانش رسمی INOTEX (۱۵ رکورد دوزبانه، ۱۱۷ پرسش، ۵۹ مترادف) با
     منبع‌دهی به inotex.com و ثبت تعارض‌ها در صف بازبینی.
  3. منیفست منابع + اسکریپت تازه‌سازی/تشخیص تغییر + snapshot.
  4. تم `inotex` (پالت رسمی، طراحی آجری، لودر، بدون ماسکوت) با حفظ اسکلت.
  5. حذف رابط pet/companion از تم liquid-glass و mount مربوط به آن.
  6. فعال‌سازی پیش‌فرض بازیابی امبدینگ محلی + سه اصلاح ریشه‌ای اندازه‌گیری‌شده
     (مترادف هویتی، Tier 0، کالیبراسیون) — سند improvement-log.md.
  7. هارنس ارزیابی + مجموعهٔ طلایی ۶۰ پرسشی + اجرای بنچمارک دو بک‌اند.
  8. لایهٔ providers، endpoint آمادگی /api/ready، نسخهٔ دانش، لاگ JSON.
  9. تست‌های جدید seed و به‌روزرسانی تست‌های UI مطابق سیاست بدون-ماسکوت.
  10. مستندات مهندسی و بستهٔ شواهد دانش‌بنیان.
- **پچ‌های ردشده/بازگردانده:** اجرای مستقیم اسکریپت reset روی DB (به‌جای آن
  مسیر غیرمخربِ انتقال DB به پشتیبان و بازتولید انتخاب شد).
- **راستی‌آزمایی ماشینی همین نشست:** ۵۱ تست pytest سبز؛ بنچمارک بازیابی
  اجرا و ذخیره شد؛ QA بصری دسکتاپ/موبایل با اسکرین‌شات؛ smoke زندهٔ /chat.
- **بازبینی انسانی:** pending — هیچ بازبینی انسانی‌ای هنوز انجام نشده و
  ادعا نمی‌شود. چک‌لیست: HUMAN_REVIEW_CHECKLIST.md.

## نشست ۱۴۰۵/۰۵/۲۳ (2026-08-14) — فاز دوم: تأیید OTP + Pet-INOTEX

- **مدل/ارکستراتور:** Claude Opus 5 (claude-opus-5). از این نشست به بعد
  ارکستراسیون پروژه با Opus است؛ نقش منطقی «Fable» با مدل fable-5 جایگزین شد.
  این تغییر مدل است، نه تغییر قواعد پروژه.
- **کارهای انجام‌شده (پچ‌های پذیرفته‌شده):**
  1. ماژول اختیاری `otp`: سرویس + روتر + صفحهٔ `/verify` (خارج از اسکلت چت).
  2. امنیت OTP: HMAC کلیددار، CSPRNG، مقایسهٔ constant-time، single-use،
     انقضا/تلاش/ارسال مجدد/سقف شماره — همه سمت سرور؛ provider seam با
     providerِ dev که در تولید refuse می‌کند.
  3. رابط OTP بازسازی‌شدهٔ معنایی از روی `image/otp.PNG` (بدون raster).
  4. ایمپورت Pet-INOTEX اصیل + رندرر canvas با جزئیات منتقل‌شده از موتور
     Pet-Inotex؛ فقط روی تجربهٔ OTP، سیاست بدون-ماسکوت در بقیهٔ صفحات پابرجا.
  5. یکسان‌سازی اندازهٔ دکمه‌های کامپوزر چت (۴۲px) پس از ایمپورت آواتار.
  6. خارج‌کردن پوشهٔ Elecomp-Pet از پروژه پس از خواندن دادهٔ موتور آن.
- **پچ‌های ردشده:** تولید تصویر HD/پوز جدید — هیچ مدل تصویری در دسترس نبود
  (جزئیات در `docs/features/otp-verification/RESEARCH.md`).
- **راستی‌آزمایی ماشینی:** ۶۷ تست pytest سبز (۵۱ قبلی + ۱۶ تست OTP)؛
  چرخهٔ کامل OTP در مرورگر زنده (کد غلط/انقضا/ارسال مجدد/موفقیت) با
  اسکرین‌شات دسکتاپ و موبایل.
- **بازبینی انسانی:** pending.

## نشست‌های پیش از این تاریخ
کارهای قبلی (ساخت اولیهٔ CMS، سیستم ماژول، تم liquid-glass، امبدینگ اولیه)
نیز با کمک AI و توسط عامل‌های قبلی انجام شده و در تاریخچهٔ git ثبت است.
