# چک‌لیست بازبینی انسانی — پیش از هر انتشار

هیچ نسخه‌ای بدون تیک‌خوردن این موارد توسط یک انسان مسئول منتشر نمی‌شود.
(وضعیت فعلی همهٔ موارد: انجام‌نشده — pending)

## محتوا
- [ ] همهٔ واقعیت‌های `app/default_content.py` با صفحات زندهٔ inotex.com تطبیق داده شد
- [ ] موارد `content/review-queue.md` (تعارض ساعت، فهرست برگزارکنندگان، مسیر بازدید) بسته شد
- [ ] پرسش‌های synthetic مجموعهٔ طلایی بازبینی و برچسب انسانی خورد
- [ ] ترجمه‌های انگلیسی بازخوانی شد

## فنی
- [ ] دیف کامل نشست AI خوانده و امضا شد (git diff)
- [ ] نتایج بنچمارک بازتولید شد (`scripts/run_eval.py`)
- [ ] تست‌ها روی ماشین تمیز سبز شد (`pytest`)
- [ ] مسیر پشتیبان/بازیابی DB یک بار واقعی تمرین شد
- [ ] متغیرهای `.env` تولید (SECRET_KEY، ADMIN_*، ALLOWED_ORIGINS، COOKIE_SECURE=true) ست شد

## بصری و دسترسی‌پذیری
- [ ] بازبینی دسکتاپ/موبایل/کیوسک لمسی روی دستگاه واقعی
- [ ] کنتراست متن‌های روی زرد و سطوح سرمه‌ای با ابزار WCAG سنجیده شد
- [ ] ناوبری کامل صفحه‌کلید + قارئ صفحه

## انتشار
- [ ] `ADMIN_CREDENTIALS.txt` پس از ورود اول حذف شد
- [ ] هیچ فایل داخلی (graphify-out، content/snapshots، .env) در بستهٔ انتشار نیست

نام بازبین: ______   تاریخ: ______   امضا: ______
