# Test-only dependencies — NOT needed for a customer install.
# Install with:  pip install -r requirements-dev.txt
# Then, for browser e2e tests:  playwright install chromium
#
# Kept separate from requirements.txt so customer deployments don't pull in
# pytest, Playwright, and ~150MB of browser binaries.

pytest
pytest-asyncio
pytest-playwright

# tests/test_booth_videos.py imports this at module level, and
# scripts/import-content.py reads the customer's .xlsx with it. Neither
# is part of a customer install, so it does not belong in
# requirements.txt — but without it here, collection ERRORS rather than
# skips, and the whole suite stops.
openpyxl
