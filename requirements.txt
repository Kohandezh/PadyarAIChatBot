fastapi
jinja2
uvicorn
scikit-learn
openai
python-multipart
numpy
httpx
# Already installed transitively by FastAPI/Starlette and httpx, but declared
# because app/services/ai/adapters/base.py imports it directly to run the
# blocking DNS resolution off the event loop. An undeclared direct import
# breaks the day a transitive dependency drops it.
anyio
# PostgreSQL is the PRODUCTION backend — app/prodcheck.py refuses to start a
# production install on anything else — and app/db/pg.py imports psycopg at
# module level. It was never declared here, so `pip install -r requirements.txt`
# produced an install that could not start in its own supported configuration,
# and CI could not import the db layer at all. [binary] avoids needing a
# compiler toolchain on the deploy host.
psycopg[binary]
psycopg-pool
python-dotenv
bcrypt
cryptography
model2vec
# Pure-Python QR encoder used by the exhibition lead-capture module to render
# the company edit invite as an on-screen SVG the contact scans at the booth.
# No Pillow: the SVG factory needs no imaging library, so this adds ~45 KB and
# nothing else. It exists because the SMS channel cannot be relied on to carry
# a link (see app/services/sms.py, Asanak error 1014).
qrcode
